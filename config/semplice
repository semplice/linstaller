[linstaller]
distribution = Semplice
frontend = cli
modules = welcome.front timezone.front userhost.front partdisks.front bootloader.front summary.front partdisks.inst unsquash.inst configure.inst bootloader.inst end.front
min_size = 1

[module:unsquash]
image = /live/image/live/filesystem.squashfs
