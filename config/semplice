## MAIN LINSTALLER CONFIGURATION FILE FOR SEMPLICE

[linstaller:extends]
source = semplice-base

# Do not ask for language, use the system's one instead.
[module:language]
ask = False

# Disable root
[module:userhost]
root = None

# We support only grub currently
[module:bootloader]
bootloader = grub
