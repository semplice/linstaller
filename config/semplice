[linstaller]
distribution = Semplice
frontend = cli
modules = welcome.front language.front timezone.front userhost.front partdisks.front bootloader.front summary.front partdisks.inst unsquash.inst language.inst timezone.inst userhost.inst fstab.inst debian.inst bootloader.inst end.front
special = partdisks.inst unsquash.inst
min_size = 1

[module:bootloader]
bootloader = grub

[module:unsquash]
image = /live/image/live/filesystem.squashfs

[module:debian]
remove = /live/image/live/filesystem.packages-remove
