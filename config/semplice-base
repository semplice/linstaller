## BASE LINSTALLER CONFIGURATION FILE FOR SEMPLICE -- END USER WILL NOT (HOPEFULLY) NEED THIS

[linstaller]
distribution = Semplice
modules = welcome.front language.front timezone.front userhost.front partdisks.front bootloader.front summary.front partdisks.inst unsquash.inst language.inst timezone.inst userhost.inst fstab.inst debian.inst semplice.inst bootloader.inst end.front
special = partdisks.inst unsquash.inst

[module:unsquash]
image = /live/image/live/filesystem.squashfs

[module:debian]
remove = /live/image/live/filesystem.packages-remove
